/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { REGTECH_PROGRAM_ADDRESS } from "../programs";

export const SET_PARTNER_ACTIVE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([189, 103, 121, 122, 196, 186, 246, 123]);

export function getSetPartnerActiveDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_PARTNER_ACTIVE_DISCRIMINATOR,
  );
}

export type SetPartnerActiveInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPartner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPartner extends string
        ? WritableAccount<TAccountPartner>
        : TAccountPartner,
      ...TRemainingAccounts,
    ]
  >;

export type SetPartnerActiveInstructionData = {
  discriminator: ReadonlyUint8Array;
  active: boolean;
  reasonCode: number;
};

export type SetPartnerActiveInstructionDataArgs = {
  active: boolean;
  reasonCode: number;
};

export function getSetPartnerActiveInstructionDataEncoder(): FixedSizeEncoder<SetPartnerActiveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["active", getBooleanEncoder()],
      ["reasonCode", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_PARTNER_ACTIVE_DISCRIMINATOR }),
  );
}

export function getSetPartnerActiveInstructionDataDecoder(): FixedSizeDecoder<SetPartnerActiveInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["active", getBooleanDecoder()],
    ["reasonCode", getU8Decoder()],
  ]);
}

export function getSetPartnerActiveInstructionDataCodec(): FixedSizeCodec<
  SetPartnerActiveInstructionDataArgs,
  SetPartnerActiveInstructionData
> {
  return combineCodec(
    getSetPartnerActiveInstructionDataEncoder(),
    getSetPartnerActiveInstructionDataDecoder(),
  );
}

export type SetPartnerActiveAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountPartner extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  partner: Address<TAccountPartner>;
  active: SetPartnerActiveInstructionDataArgs["active"];
  reasonCode: SetPartnerActiveInstructionDataArgs["reasonCode"];
};

export async function getSetPartnerActiveInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountPartner extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: SetPartnerActiveAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetPartnerActiveInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("partner", accounts.partner),
    ],
    data: getSetPartnerActiveInstructionDataEncoder().encode(
      args as SetPartnerActiveInstructionDataArgs,
    ),
    programAddress,
  } as SetPartnerActiveInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner
  >);
}

export type SetPartnerActiveInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountPartner extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  partner: Address<TAccountPartner>;
  active: SetPartnerActiveInstructionDataArgs["active"];
  reasonCode: SetPartnerActiveInstructionDataArgs["reasonCode"];
};

export function getSetPartnerActiveInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountPartner extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: SetPartnerActiveInput<TAccountAdmin, TAccountConfig, TAccountPartner>,
  config?: { programAddress?: TProgramAddress },
): SetPartnerActiveInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountPartner
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("partner", accounts.partner),
    ],
    data: getSetPartnerActiveInstructionDataEncoder().encode(
      args as SetPartnerActiveInstructionDataArgs,
    ),
    programAddress,
  } as SetPartnerActiveInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner
  >);
}

export type ParsedSetPartnerActiveInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    partner: TAccountMetas[2];
  };
  data: SetPartnerActiveInstructionData;
};

export function parseSetPartnerActiveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetPartnerActiveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      partner: getNextAccount(),
    },
    data: getSetPartnerActiveInstructionDataDecoder().decode(instruction.data),
  };
}
