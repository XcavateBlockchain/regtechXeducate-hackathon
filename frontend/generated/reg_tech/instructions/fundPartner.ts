/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { REGTECH_PROGRAM_ADDRESS } from "../programs";

export const FUND_PARTNER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  78, 188, 157, 78, 216, 47, 168, 175,
]);

export function getFundPartnerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FUND_PARTNER_DISCRIMINATOR,
  );
}

export type FundPartnerInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPartner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPartner extends string
        ? WritableAccount<TAccountPartner>
        : TAccountPartner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FundPartnerInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type FundPartnerInstructionDataArgs = { amount: number | bigint };

export function getFundPartnerInstructionDataEncoder(): FixedSizeEncoder<FundPartnerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: FUND_PARTNER_DISCRIMINATOR }),
  );
}

export function getFundPartnerInstructionDataDecoder(): FixedSizeDecoder<FundPartnerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getFundPartnerInstructionDataCodec(): FixedSizeCodec<
  FundPartnerInstructionDataArgs,
  FundPartnerInstructionData
> {
  return combineCodec(
    getFundPartnerInstructionDataEncoder(),
    getFundPartnerInstructionDataDecoder(),
  );
}

export type FundPartnerAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountPartner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  partner: Address<TAccountPartner>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: FundPartnerInstructionDataArgs["amount"];
};

export async function getFundPartnerInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountPartner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: FundPartnerAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  FundPartnerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("partner", accounts.partner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getFundPartnerInstructionDataEncoder().encode(
      args as FundPartnerInstructionDataArgs,
    ),
    programAddress,
  } as FundPartnerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner,
    TAccountSystemProgram
  >);
}

export type FundPartnerInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountPartner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  partner: Address<TAccountPartner>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: FundPartnerInstructionDataArgs["amount"];
};

export function getFundPartnerInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountPartner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: FundPartnerInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): FundPartnerInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountPartner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("partner", accounts.partner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getFundPartnerInstructionDataEncoder().encode(
      args as FundPartnerInstructionDataArgs,
    ),
    programAddress,
  } as FundPartnerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner,
    TAccountSystemProgram
  >);
}

export type ParsedFundPartnerInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    partner: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: FundPartnerInstructionData;
};

export function parseFundPartnerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFundPartnerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      partner: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getFundPartnerInstructionDataDecoder().decode(instruction.data),
  };
}
