/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findPartnerPda } from "../pdas";
import { REGTECH_PROGRAM_ADDRESS } from "../programs";

export const REGISTER_PARTNER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([126, 105, 229, 224, 136, 11, 184, 137]);

export function getRegisterPartnerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_PARTNER_DISCRIMINATOR,
  );
}

export type RegisterPartnerInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPartner extends string | AccountMeta<string> = string,
  TAccountCollection extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPartner extends string
        ? WritableAccount<TAccountPartner>
        : TAccountPartner,
      TAccountCollection extends string
        ? ReadonlyAccount<TAccountCollection>
        : TAccountCollection,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterPartnerInstructionData = {
  discriminator: ReadonlyUint8Array;
  partnerId: ReadonlyUint8Array;
  name: string;
  attestor: Address;
  partnerAdmin: Address;
  passThresholdBpsOverride: Option<number>;
  cooldownSecondsOverride: Option<bigint>;
};

export type RegisterPartnerInstructionDataArgs = {
  partnerId: ReadonlyUint8Array;
  name: string;
  attestor: Address;
  partnerAdmin: Address;
  passThresholdBpsOverride: OptionOrNullable<number>;
  cooldownSecondsOverride: OptionOrNullable<number | bigint>;
};

export function getRegisterPartnerInstructionDataEncoder(): Encoder<RegisterPartnerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["partnerId", fixEncoderSize(getBytesEncoder(), 16)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["attestor", getAddressEncoder()],
      ["partnerAdmin", getAddressEncoder()],
      ["passThresholdBpsOverride", getOptionEncoder(getU16Encoder())],
      ["cooldownSecondsOverride", getOptionEncoder(getI64Encoder())],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_PARTNER_DISCRIMINATOR }),
  );
}

export function getRegisterPartnerInstructionDataDecoder(): Decoder<RegisterPartnerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["partnerId", fixDecoderSize(getBytesDecoder(), 16)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["attestor", getAddressDecoder()],
    ["partnerAdmin", getAddressDecoder()],
    ["passThresholdBpsOverride", getOptionDecoder(getU16Decoder())],
    ["cooldownSecondsOverride", getOptionDecoder(getI64Decoder())],
  ]);
}

export function getRegisterPartnerInstructionDataCodec(): Codec<
  RegisterPartnerInstructionDataArgs,
  RegisterPartnerInstructionData
> {
  return combineCodec(
    getRegisterPartnerInstructionDataEncoder(),
    getRegisterPartnerInstructionDataDecoder(),
  );
}

export type RegisterPartnerAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountPartner extends string = string,
  TAccountCollection extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  partner?: Address<TAccountPartner>;
  /**
   * credentials. We can't use a typed Anchor account here because mpl-core
   * isn't Anchor-native, so the handler checks the owner, the account type,
   * and the update authority by hand before trusting it.
   */
  collection: Address<TAccountCollection>;
  systemProgram?: Address<TAccountSystemProgram>;
  partnerId: RegisterPartnerInstructionDataArgs["partnerId"];
  name: RegisterPartnerInstructionDataArgs["name"];
  attestor: RegisterPartnerInstructionDataArgs["attestor"];
  partnerAdmin: RegisterPartnerInstructionDataArgs["partnerAdmin"];
  passThresholdBpsOverride: RegisterPartnerInstructionDataArgs["passThresholdBpsOverride"];
  cooldownSecondsOverride: RegisterPartnerInstructionDataArgs["cooldownSecondsOverride"];
};

export async function getRegisterPartnerInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountPartner extends string,
  TAccountCollection extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: RegisterPartnerAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner,
    TAccountCollection,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterPartnerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner,
    TAccountCollection,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: true },
    collection: { value: input.collection ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.partner.value) {
    accounts.partner.value = await findPartnerPda({
      partnerId: getNonNullResolvedInstructionInput(
        "partnerId",
        args.partnerId,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("partner", accounts.partner),
      getAccountMeta("collection", accounts.collection),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterPartnerInstructionDataEncoder().encode(
      args as RegisterPartnerInstructionDataArgs,
    ),
    programAddress,
  } as RegisterPartnerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner,
    TAccountCollection,
    TAccountSystemProgram
  >);
}

export type RegisterPartnerInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountPartner extends string = string,
  TAccountCollection extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  partner: Address<TAccountPartner>;
  /**
   * credentials. We can't use a typed Anchor account here because mpl-core
   * isn't Anchor-native, so the handler checks the owner, the account type,
   * and the update authority by hand before trusting it.
   */
  collection: Address<TAccountCollection>;
  systemProgram?: Address<TAccountSystemProgram>;
  partnerId: RegisterPartnerInstructionDataArgs["partnerId"];
  name: RegisterPartnerInstructionDataArgs["name"];
  attestor: RegisterPartnerInstructionDataArgs["attestor"];
  partnerAdmin: RegisterPartnerInstructionDataArgs["partnerAdmin"];
  passThresholdBpsOverride: RegisterPartnerInstructionDataArgs["passThresholdBpsOverride"];
  cooldownSecondsOverride: RegisterPartnerInstructionDataArgs["cooldownSecondsOverride"];
};

export function getRegisterPartnerInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountPartner extends string,
  TAccountCollection extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: RegisterPartnerInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner,
    TAccountCollection,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterPartnerInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountPartner,
  TAccountCollection,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: true },
    collection: { value: input.collection ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("partner", accounts.partner),
      getAccountMeta("collection", accounts.collection),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterPartnerInstructionDataEncoder().encode(
      args as RegisterPartnerInstructionDataArgs,
    ),
    programAddress,
  } as RegisterPartnerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner,
    TAccountCollection,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterPartnerInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    partner: TAccountMetas[2];
    /**
     * credentials. We can't use a typed Anchor account here because mpl-core
     * isn't Anchor-native, so the handler checks the owner, the account type,
     * and the update authority by hand before trusting it.
     */
    collection: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RegisterPartnerInstructionData;
};

export function parseRegisterPartnerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterPartnerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      partner: getNextAccount(),
      collection: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterPartnerInstructionDataDecoder().decode(instruction.data),
  };
}
