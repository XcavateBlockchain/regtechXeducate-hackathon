/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { REGTECH_PROGRAM_ADDRESS } from "../programs";

export const START_ATTEMPT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  31, 142, 37, 110, 117, 122, 225, 127,
]);

export function getStartAttemptDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    START_ATTEMPT_DISCRIMINATOR,
  );
}

export type StartAttemptInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountAttestor extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPartner extends string | AccountMeta<string> = string,
  TAccountModule extends string | AccountMeta<string> = string,
  TAccountEnrollment extends string | AccountMeta<string> = string,
  TAccountAttempt extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAttestor extends string
        ? ReadonlySignerAccount<TAccountAttestor> &
            AccountSignerMeta<TAccountAttestor>
        : TAccountAttestor,
      TAccountUser extends string
        ? ReadonlyAccount<TAccountUser>
        : TAccountUser,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPartner extends string
        ? WritableAccount<TAccountPartner>
        : TAccountPartner,
      TAccountModule extends string
        ? ReadonlyAccount<TAccountModule>
        : TAccountModule,
      TAccountEnrollment extends string
        ? ReadonlyAccount<TAccountEnrollment>
        : TAccountEnrollment,
      TAccountAttempt extends string
        ? WritableAccount<TAccountAttempt>
        : TAccountAttempt,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type StartAttemptInstructionData = { discriminator: ReadonlyUint8Array };

export type StartAttemptInstructionDataArgs = {};

export function getStartAttemptInstructionDataEncoder(): FixedSizeEncoder<StartAttemptInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: START_ATTEMPT_DISCRIMINATOR }),
  );
}

export function getStartAttemptInstructionDataDecoder(): FixedSizeDecoder<StartAttemptInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getStartAttemptInstructionDataCodec(): FixedSizeCodec<
  StartAttemptInstructionDataArgs,
  StartAttemptInstructionData
> {
  return combineCodec(
    getStartAttemptInstructionDataEncoder(),
    getStartAttemptInstructionDataDecoder(),
  );
}

export type StartAttemptAsyncInput<
  TAccountAttestor extends string = string,
  TAccountUser extends string = string,
  TAccountConfig extends string = string,
  TAccountPartner extends string = string,
  TAccountModule extends string = string,
  TAccountEnrollment extends string = string,
  TAccountAttempt extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  attestor: TransactionSigner<TAccountAttestor>;
  /**
   * audit trail. Doesn't sign. Authorization comes from the attestor
   * (via has_one on Partner) and the Enrollment PDA that partner_admin
   * had to create earlier.
   */
  user: Address<TAccountUser>;
  config?: Address<TAccountConfig>;
  partner: Address<TAccountPartner>;
  module: Address<TAccountModule>;
  enrollment: Address<TAccountEnrollment>;
  attempt: Address<TAccountAttempt>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getStartAttemptInstructionAsync<
  TAccountAttestor extends string,
  TAccountUser extends string,
  TAccountConfig extends string,
  TAccountPartner extends string,
  TAccountModule extends string,
  TAccountEnrollment extends string,
  TAccountAttempt extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: StartAttemptAsyncInput<
    TAccountAttestor,
    TAccountUser,
    TAccountConfig,
    TAccountPartner,
    TAccountModule,
    TAccountEnrollment,
    TAccountAttempt,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  StartAttemptInstruction<
    TProgramAddress,
    TAccountAttestor,
    TAccountUser,
    TAccountConfig,
    TAccountPartner,
    TAccountModule,
    TAccountEnrollment,
    TAccountAttempt,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    attestor: { value: input.attestor ?? null, isWritable: false },
    user: { value: input.user ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: true },
    module: { value: input.module ?? null, isWritable: false },
    enrollment: { value: input.enrollment ?? null, isWritable: false },
    attempt: { value: input.attempt ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("attestor", accounts.attestor),
      getAccountMeta("user", accounts.user),
      getAccountMeta("config", accounts.config),
      getAccountMeta("partner", accounts.partner),
      getAccountMeta("module", accounts.module),
      getAccountMeta("enrollment", accounts.enrollment),
      getAccountMeta("attempt", accounts.attempt),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getStartAttemptInstructionDataEncoder().encode({}),
    programAddress,
  } as StartAttemptInstruction<
    TProgramAddress,
    TAccountAttestor,
    TAccountUser,
    TAccountConfig,
    TAccountPartner,
    TAccountModule,
    TAccountEnrollment,
    TAccountAttempt,
    TAccountSystemProgram
  >);
}

export type StartAttemptInput<
  TAccountAttestor extends string = string,
  TAccountUser extends string = string,
  TAccountConfig extends string = string,
  TAccountPartner extends string = string,
  TAccountModule extends string = string,
  TAccountEnrollment extends string = string,
  TAccountAttempt extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  attestor: TransactionSigner<TAccountAttestor>;
  /**
   * audit trail. Doesn't sign. Authorization comes from the attestor
   * (via has_one on Partner) and the Enrollment PDA that partner_admin
   * had to create earlier.
   */
  user: Address<TAccountUser>;
  config: Address<TAccountConfig>;
  partner: Address<TAccountPartner>;
  module: Address<TAccountModule>;
  enrollment: Address<TAccountEnrollment>;
  attempt: Address<TAccountAttempt>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getStartAttemptInstruction<
  TAccountAttestor extends string,
  TAccountUser extends string,
  TAccountConfig extends string,
  TAccountPartner extends string,
  TAccountModule extends string,
  TAccountEnrollment extends string,
  TAccountAttempt extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: StartAttemptInput<
    TAccountAttestor,
    TAccountUser,
    TAccountConfig,
    TAccountPartner,
    TAccountModule,
    TAccountEnrollment,
    TAccountAttempt,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): StartAttemptInstruction<
  TProgramAddress,
  TAccountAttestor,
  TAccountUser,
  TAccountConfig,
  TAccountPartner,
  TAccountModule,
  TAccountEnrollment,
  TAccountAttempt,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    attestor: { value: input.attestor ?? null, isWritable: false },
    user: { value: input.user ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: true },
    module: { value: input.module ?? null, isWritable: false },
    enrollment: { value: input.enrollment ?? null, isWritable: false },
    attempt: { value: input.attempt ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("attestor", accounts.attestor),
      getAccountMeta("user", accounts.user),
      getAccountMeta("config", accounts.config),
      getAccountMeta("partner", accounts.partner),
      getAccountMeta("module", accounts.module),
      getAccountMeta("enrollment", accounts.enrollment),
      getAccountMeta("attempt", accounts.attempt),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getStartAttemptInstructionDataEncoder().encode({}),
    programAddress,
  } as StartAttemptInstruction<
    TProgramAddress,
    TAccountAttestor,
    TAccountUser,
    TAccountConfig,
    TAccountPartner,
    TAccountModule,
    TAccountEnrollment,
    TAccountAttempt,
    TAccountSystemProgram
  >);
}

export type ParsedStartAttemptInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    attestor: TAccountMetas[0];
    /**
     * audit trail. Doesn't sign. Authorization comes from the attestor
     * (via has_one on Partner) and the Enrollment PDA that partner_admin
     * had to create earlier.
     */
    user: TAccountMetas[1];
    config: TAccountMetas[2];
    partner: TAccountMetas[3];
    module: TAccountMetas[4];
    enrollment: TAccountMetas[5];
    attempt: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: StartAttemptInstructionData;
};

export function parseStartAttemptInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedStartAttemptInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      attestor: getNextAccount(),
      user: getNextAccount(),
      config: getNextAccount(),
      partner: getNextAccount(),
      module: getNextAccount(),
      enrollment: getNextAccount(),
      attempt: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getStartAttemptInstructionDataDecoder().decode(instruction.data),
  };
}
