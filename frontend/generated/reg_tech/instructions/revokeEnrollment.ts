/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { REGTECH_PROGRAM_ADDRESS } from "../programs";

export const REVOKE_ENROLLMENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([153, 110, 55, 247, 206, 50, 202, 244]);

export function getRevokeEnrollmentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVOKE_ENROLLMENT_DISCRIMINATOR,
  );
}

export type RevokeEnrollmentInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountPartnerAdmin extends string | AccountMeta<string> = string,
  TAccountPartner extends string | AccountMeta<string> = string,
  TAccountEnrollment extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPartnerAdmin extends string
        ? ReadonlySignerAccount<TAccountPartnerAdmin> &
            AccountSignerMeta<TAccountPartnerAdmin>
        : TAccountPartnerAdmin,
      TAccountPartner extends string
        ? WritableAccount<TAccountPartner>
        : TAccountPartner,
      TAccountEnrollment extends string
        ? WritableAccount<TAccountEnrollment>
        : TAccountEnrollment,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeEnrollmentInstructionData = {
  discriminator: ReadonlyUint8Array;
  reasonCode: number;
};

export type RevokeEnrollmentInstructionDataArgs = { reasonCode: number };

export function getRevokeEnrollmentInstructionDataEncoder(): FixedSizeEncoder<RevokeEnrollmentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["reasonCode", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REVOKE_ENROLLMENT_DISCRIMINATOR }),
  );
}

export function getRevokeEnrollmentInstructionDataDecoder(): FixedSizeDecoder<RevokeEnrollmentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["reasonCode", getU8Decoder()],
  ]);
}

export function getRevokeEnrollmentInstructionDataCodec(): FixedSizeCodec<
  RevokeEnrollmentInstructionDataArgs,
  RevokeEnrollmentInstructionData
> {
  return combineCodec(
    getRevokeEnrollmentInstructionDataEncoder(),
    getRevokeEnrollmentInstructionDataDecoder(),
  );
}

export type RevokeEnrollmentInput<
  TAccountPartnerAdmin extends string = string,
  TAccountPartner extends string = string,
  TAccountEnrollment extends string = string,
> = {
  partnerAdmin: TransactionSigner<TAccountPartnerAdmin>;
  partner: Address<TAccountPartner>;
  enrollment: Address<TAccountEnrollment>;
  reasonCode: RevokeEnrollmentInstructionDataArgs["reasonCode"];
};

export function getRevokeEnrollmentInstruction<
  TAccountPartnerAdmin extends string,
  TAccountPartner extends string,
  TAccountEnrollment extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: RevokeEnrollmentInput<
    TAccountPartnerAdmin,
    TAccountPartner,
    TAccountEnrollment
  >,
  config?: { programAddress?: TProgramAddress },
): RevokeEnrollmentInstruction<
  TProgramAddress,
  TAccountPartnerAdmin,
  TAccountPartner,
  TAccountEnrollment
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    partnerAdmin: { value: input.partnerAdmin ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: true },
    enrollment: { value: input.enrollment ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("partnerAdmin", accounts.partnerAdmin),
      getAccountMeta("partner", accounts.partner),
      getAccountMeta("enrollment", accounts.enrollment),
    ],
    data: getRevokeEnrollmentInstructionDataEncoder().encode(
      args as RevokeEnrollmentInstructionDataArgs,
    ),
    programAddress,
  } as RevokeEnrollmentInstruction<
    TProgramAddress,
    TAccountPartnerAdmin,
    TAccountPartner,
    TAccountEnrollment
  >);
}

export type ParsedRevokeEnrollmentInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    partnerAdmin: TAccountMetas[0];
    partner: TAccountMetas[1];
    enrollment: TAccountMetas[2];
  };
  data: RevokeEnrollmentInstructionData;
};

export function parseRevokeEnrollmentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeEnrollmentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      partnerAdmin: getNextAccount(),
      partner: getNextAccount(),
      enrollment: getNextAccount(),
    },
    data: getRevokeEnrollmentInstructionDataDecoder().decode(instruction.data),
  };
}
