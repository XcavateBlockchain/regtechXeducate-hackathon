/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { REGTECH_PROGRAM_ADDRESS } from "../programs";

export const ACCEPT_ADMIN_UPDATE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([235, 252, 200, 186, 168, 233, 138, 233]);

export function getAcceptAdminUpdateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ACCEPT_ADMIN_UPDATE_DISCRIMINATOR,
  );
}

export type AcceptAdminUpdateInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountNewAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountNewAdmin extends string
        ? ReadonlySignerAccount<TAccountNewAdmin> &
            AccountSignerMeta<TAccountNewAdmin>
        : TAccountNewAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type AcceptAdminUpdateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AcceptAdminUpdateInstructionDataArgs = {};

export function getAcceptAdminUpdateInstructionDataEncoder(): FixedSizeEncoder<AcceptAdminUpdateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ACCEPT_ADMIN_UPDATE_DISCRIMINATOR }),
  );
}

export function getAcceptAdminUpdateInstructionDataDecoder(): FixedSizeDecoder<AcceptAdminUpdateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAcceptAdminUpdateInstructionDataCodec(): FixedSizeCodec<
  AcceptAdminUpdateInstructionDataArgs,
  AcceptAdminUpdateInstructionData
> {
  return combineCodec(
    getAcceptAdminUpdateInstructionDataEncoder(),
    getAcceptAdminUpdateInstructionDataDecoder(),
  );
}

export type AcceptAdminUpdateAsyncInput<
  TAccountNewAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  newAdmin: TransactionSigner<TAccountNewAdmin>;
  config?: Address<TAccountConfig>;
};

export async function getAcceptAdminUpdateInstructionAsync<
  TAccountNewAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: AcceptAdminUpdateAsyncInput<TAccountNewAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AcceptAdminUpdateInstruction<
    TProgramAddress,
    TAccountNewAdmin,
    TAccountConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    newAdmin: { value: input.newAdmin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("newAdmin", accounts.newAdmin),
      getAccountMeta("config", accounts.config),
    ],
    data: getAcceptAdminUpdateInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptAdminUpdateInstruction<
    TProgramAddress,
    TAccountNewAdmin,
    TAccountConfig
  >);
}

export type AcceptAdminUpdateInput<
  TAccountNewAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  newAdmin: TransactionSigner<TAccountNewAdmin>;
  config: Address<TAccountConfig>;
};

export function getAcceptAdminUpdateInstruction<
  TAccountNewAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: AcceptAdminUpdateInput<TAccountNewAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): AcceptAdminUpdateInstruction<
  TProgramAddress,
  TAccountNewAdmin,
  TAccountConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    newAdmin: { value: input.newAdmin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("newAdmin", accounts.newAdmin),
      getAccountMeta("config", accounts.config),
    ],
    data: getAcceptAdminUpdateInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptAdminUpdateInstruction<
    TProgramAddress,
    TAccountNewAdmin,
    TAccountConfig
  >);
}

export type ParsedAcceptAdminUpdateInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    newAdmin: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: AcceptAdminUpdateInstructionData;
};

export function parseAcceptAdminUpdateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAcceptAdminUpdateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { newAdmin: getNextAccount(), config: getNextAccount() },
    data: getAcceptAdminUpdateInstructionDataDecoder().decode(instruction.data),
  };
}
