/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { REGTECH_PROGRAM_ADDRESS } from "../programs";

export const ALLOCATE_QUIZZES_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([39, 213, 60, 52, 15, 12, 21, 230]);

export function getAllocateQuizzesDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ALLOCATE_QUIZZES_DISCRIMINATOR,
  );
}

export type AllocateQuizzesInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPartner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPartner extends string
        ? WritableAccount<TAccountPartner>
        : TAccountPartner,
      ...TRemainingAccounts,
    ]
  >;

export type AllocateQuizzesInstructionData = {
  discriminator: ReadonlyUint8Array;
  count: bigint;
};

export type AllocateQuizzesInstructionDataArgs = { count: number | bigint };

export function getAllocateQuizzesInstructionDataEncoder(): FixedSizeEncoder<AllocateQuizzesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["count", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ALLOCATE_QUIZZES_DISCRIMINATOR }),
  );
}

export function getAllocateQuizzesInstructionDataDecoder(): FixedSizeDecoder<AllocateQuizzesInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["count", getU64Decoder()],
  ]);
}

export function getAllocateQuizzesInstructionDataCodec(): FixedSizeCodec<
  AllocateQuizzesInstructionDataArgs,
  AllocateQuizzesInstructionData
> {
  return combineCodec(
    getAllocateQuizzesInstructionDataEncoder(),
    getAllocateQuizzesInstructionDataDecoder(),
  );
}

export type AllocateQuizzesAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountPartner extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  partner: Address<TAccountPartner>;
  count: AllocateQuizzesInstructionDataArgs["count"];
};

export async function getAllocateQuizzesInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountPartner extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: AllocateQuizzesAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AllocateQuizzesInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("partner", accounts.partner),
    ],
    data: getAllocateQuizzesInstructionDataEncoder().encode(
      args as AllocateQuizzesInstructionDataArgs,
    ),
    programAddress,
  } as AllocateQuizzesInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner
  >);
}

export type AllocateQuizzesInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountPartner extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  partner: Address<TAccountPartner>;
  count: AllocateQuizzesInstructionDataArgs["count"];
};

export function getAllocateQuizzesInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountPartner extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: AllocateQuizzesInput<TAccountAdmin, TAccountConfig, TAccountPartner>,
  config?: { programAddress?: TProgramAddress },
): AllocateQuizzesInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountPartner
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("partner", accounts.partner),
    ],
    data: getAllocateQuizzesInstructionDataEncoder().encode(
      args as AllocateQuizzesInstructionDataArgs,
    ),
    programAddress,
  } as AllocateQuizzesInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountPartner
  >);
}

export type ParsedAllocateQuizzesInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    partner: TAccountMetas[2];
  };
  data: AllocateQuizzesInstructionData;
};

export function parseAllocateQuizzesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAllocateQuizzesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      partner: getNextAccount(),
    },
    data: getAllocateQuizzesInstructionDataDecoder().decode(instruction.data),
  };
}
