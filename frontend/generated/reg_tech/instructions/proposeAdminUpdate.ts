/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { REGTECH_PROGRAM_ADDRESS } from "../programs";

export const PROPOSE_ADMIN_UPDATE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([127, 181, 95, 50, 34, 82, 19, 110]);

export function getProposeAdminUpdateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_ADMIN_UPDATE_DISCRIMINATOR,
  );
}

export type ProposeAdminUpdateInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeAdminUpdateInstructionData = {
  discriminator: ReadonlyUint8Array;
  candidate: Address;
};

export type ProposeAdminUpdateInstructionDataArgs = { candidate: Address };

export function getProposeAdminUpdateInstructionDataEncoder(): FixedSizeEncoder<ProposeAdminUpdateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["candidate", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PROPOSE_ADMIN_UPDATE_DISCRIMINATOR,
    }),
  );
}

export function getProposeAdminUpdateInstructionDataDecoder(): FixedSizeDecoder<ProposeAdminUpdateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["candidate", getAddressDecoder()],
  ]);
}

export function getProposeAdminUpdateInstructionDataCodec(): FixedSizeCodec<
  ProposeAdminUpdateInstructionDataArgs,
  ProposeAdminUpdateInstructionData
> {
  return combineCodec(
    getProposeAdminUpdateInstructionDataEncoder(),
    getProposeAdminUpdateInstructionDataDecoder(),
  );
}

export type ProposeAdminUpdateAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  candidate: ProposeAdminUpdateInstructionDataArgs["candidate"];
};

export async function getProposeAdminUpdateInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: ProposeAdminUpdateAsyncInput<TAccountAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProposeAdminUpdateInstruction<TProgramAddress, TAccountAdmin, TAccountConfig>
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
    ],
    data: getProposeAdminUpdateInstructionDataEncoder().encode(
      args as ProposeAdminUpdateInstructionDataArgs,
    ),
    programAddress,
  } as ProposeAdminUpdateInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig
  >);
}

export type ProposeAdminUpdateInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  candidate: ProposeAdminUpdateInstructionDataArgs["candidate"];
};

export function getProposeAdminUpdateInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: ProposeAdminUpdateInput<TAccountAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): ProposeAdminUpdateInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
    ],
    data: getProposeAdminUpdateInstructionDataEncoder().encode(
      args as ProposeAdminUpdateInstructionDataArgs,
    ),
    programAddress,
  } as ProposeAdminUpdateInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig
  >);
}

export type ParsedProposeAdminUpdateInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: ProposeAdminUpdateInstructionData;
};

export function parseProposeAdminUpdateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeAdminUpdateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), config: getNextAccount() },
    data: getProposeAdminUpdateInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
