/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { REGTECH_PROGRAM_ADDRESS } from "../programs";

export const SUBMIT_ATTEMPT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  231, 46, 227, 123, 117, 212, 219, 247,
]);

export function getSubmitAttemptDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBMIT_ATTEMPT_DISCRIMINATOR,
  );
}

export type SubmitAttemptInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountAttestor extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPartner extends string | AccountMeta<string> = string,
  TAccountModule extends string | AccountMeta<string> = string,
  TAccountAttempt extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAttestor extends string
        ? ReadonlySignerAccount<TAccountAttestor> &
            AccountSignerMeta<TAccountAttestor>
        : TAccountAttestor,
      TAccountUser extends string
        ? ReadonlyAccount<TAccountUser>
        : TAccountUser,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPartner extends string
        ? ReadonlyAccount<TAccountPartner>
        : TAccountPartner,
      TAccountModule extends string
        ? ReadonlyAccount<TAccountModule>
        : TAccountModule,
      TAccountAttempt extends string
        ? WritableAccount<TAccountAttempt>
        : TAccountAttempt,
      ...TRemainingAccounts,
    ]
  >;

export type SubmitAttemptInstructionData = {
  discriminator: ReadonlyUint8Array;
  scoreBps: number;
};

export type SubmitAttemptInstructionDataArgs = { scoreBps: number };

export function getSubmitAttemptInstructionDataEncoder(): FixedSizeEncoder<SubmitAttemptInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["scoreBps", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SUBMIT_ATTEMPT_DISCRIMINATOR }),
  );
}

export function getSubmitAttemptInstructionDataDecoder(): FixedSizeDecoder<SubmitAttemptInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["scoreBps", getU16Decoder()],
  ]);
}

export function getSubmitAttemptInstructionDataCodec(): FixedSizeCodec<
  SubmitAttemptInstructionDataArgs,
  SubmitAttemptInstructionData
> {
  return combineCodec(
    getSubmitAttemptInstructionDataEncoder(),
    getSubmitAttemptInstructionDataDecoder(),
  );
}

export type SubmitAttemptAsyncInput<
  TAccountAttestor extends string = string,
  TAccountUser extends string = string,
  TAccountConfig extends string = string,
  TAccountPartner extends string = string,
  TAccountModule extends string = string,
  TAccountAttempt extends string = string,
> = {
  attestor: TransactionSigner<TAccountAttestor>;
  /**
   * Authorization chains back through the Enrollment PDA that start_attempt
   * required, which in turn required partner_admin to sign enroll_user.
   * The attestor can't write scores against an arbitrary pubkey because
   * the Attempt PDA won't exist unless start_attempt already succeeded
   * for that user, and start_attempt won't succeed without a live
   * Enrollment. So the user's consent comes from the partner's maker-
   * checker enrollment decision, not from a direct user signature here.
   */
  user: Address<TAccountUser>;
  config?: Address<TAccountConfig>;
  partner: Address<TAccountPartner>;
  module: Address<TAccountModule>;
  attempt: Address<TAccountAttempt>;
  scoreBps: SubmitAttemptInstructionDataArgs["scoreBps"];
};

export async function getSubmitAttemptInstructionAsync<
  TAccountAttestor extends string,
  TAccountUser extends string,
  TAccountConfig extends string,
  TAccountPartner extends string,
  TAccountModule extends string,
  TAccountAttempt extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: SubmitAttemptAsyncInput<
    TAccountAttestor,
    TAccountUser,
    TAccountConfig,
    TAccountPartner,
    TAccountModule,
    TAccountAttempt
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SubmitAttemptInstruction<
    TProgramAddress,
    TAccountAttestor,
    TAccountUser,
    TAccountConfig,
    TAccountPartner,
    TAccountModule,
    TAccountAttempt
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    attestor: { value: input.attestor ?? null, isWritable: false },
    user: { value: input.user ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: false },
    module: { value: input.module ?? null, isWritable: false },
    attempt: { value: input.attempt ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("attestor", accounts.attestor),
      getAccountMeta("user", accounts.user),
      getAccountMeta("config", accounts.config),
      getAccountMeta("partner", accounts.partner),
      getAccountMeta("module", accounts.module),
      getAccountMeta("attempt", accounts.attempt),
    ],
    data: getSubmitAttemptInstructionDataEncoder().encode(
      args as SubmitAttemptInstructionDataArgs,
    ),
    programAddress,
  } as SubmitAttemptInstruction<
    TProgramAddress,
    TAccountAttestor,
    TAccountUser,
    TAccountConfig,
    TAccountPartner,
    TAccountModule,
    TAccountAttempt
  >);
}

export type SubmitAttemptInput<
  TAccountAttestor extends string = string,
  TAccountUser extends string = string,
  TAccountConfig extends string = string,
  TAccountPartner extends string = string,
  TAccountModule extends string = string,
  TAccountAttempt extends string = string,
> = {
  attestor: TransactionSigner<TAccountAttestor>;
  /**
   * Authorization chains back through the Enrollment PDA that start_attempt
   * required, which in turn required partner_admin to sign enroll_user.
   * The attestor can't write scores against an arbitrary pubkey because
   * the Attempt PDA won't exist unless start_attempt already succeeded
   * for that user, and start_attempt won't succeed without a live
   * Enrollment. So the user's consent comes from the partner's maker-
   * checker enrollment decision, not from a direct user signature here.
   */
  user: Address<TAccountUser>;
  config: Address<TAccountConfig>;
  partner: Address<TAccountPartner>;
  module: Address<TAccountModule>;
  attempt: Address<TAccountAttempt>;
  scoreBps: SubmitAttemptInstructionDataArgs["scoreBps"];
};

export function getSubmitAttemptInstruction<
  TAccountAttestor extends string,
  TAccountUser extends string,
  TAccountConfig extends string,
  TAccountPartner extends string,
  TAccountModule extends string,
  TAccountAttempt extends string,
  TProgramAddress extends Address = typeof REGTECH_PROGRAM_ADDRESS,
>(
  input: SubmitAttemptInput<
    TAccountAttestor,
    TAccountUser,
    TAccountConfig,
    TAccountPartner,
    TAccountModule,
    TAccountAttempt
  >,
  config?: { programAddress?: TProgramAddress },
): SubmitAttemptInstruction<
  TProgramAddress,
  TAccountAttestor,
  TAccountUser,
  TAccountConfig,
  TAccountPartner,
  TAccountModule,
  TAccountAttempt
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGTECH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    attestor: { value: input.attestor ?? null, isWritable: false },
    user: { value: input.user ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    partner: { value: input.partner ?? null, isWritable: false },
    module: { value: input.module ?? null, isWritable: false },
    attempt: { value: input.attempt ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("attestor", accounts.attestor),
      getAccountMeta("user", accounts.user),
      getAccountMeta("config", accounts.config),
      getAccountMeta("partner", accounts.partner),
      getAccountMeta("module", accounts.module),
      getAccountMeta("attempt", accounts.attempt),
    ],
    data: getSubmitAttemptInstructionDataEncoder().encode(
      args as SubmitAttemptInstructionDataArgs,
    ),
    programAddress,
  } as SubmitAttemptInstruction<
    TProgramAddress,
    TAccountAttestor,
    TAccountUser,
    TAccountConfig,
    TAccountPartner,
    TAccountModule,
    TAccountAttempt
  >);
}

export type ParsedSubmitAttemptInstruction<
  TProgram extends string = typeof REGTECH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    attestor: TAccountMetas[0];
    /**
     * Authorization chains back through the Enrollment PDA that start_attempt
     * required, which in turn required partner_admin to sign enroll_user.
     * The attestor can't write scores against an arbitrary pubkey because
     * the Attempt PDA won't exist unless start_attempt already succeeded
     * for that user, and start_attempt won't succeed without a live
     * Enrollment. So the user's consent comes from the partner's maker-
     * checker enrollment decision, not from a direct user signature here.
     */
    user: TAccountMetas[1];
    config: TAccountMetas[2];
    partner: TAccountMetas[3];
    module: TAccountMetas[4];
    attempt: TAccountMetas[5];
  };
  data: SubmitAttemptInstructionData;
};

export function parseSubmitAttemptInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSubmitAttemptInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      attestor: getNextAccount(),
      user: getNextAccount(),
      config: getNextAccount(),
      partner: getNextAccount(),
      module: getNextAccount(),
      attempt: getNextAccount(),
    },
    data: getSubmitAttemptInstructionDataDecoder().decode(instruction.data),
  };
}
